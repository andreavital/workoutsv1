# Fitia Workouts · prototype v1

Prototipo funcional (HTML único, sin dependencias). `index.html` es todo el proyecto.

- Deploy: importar el repo en Vercel como sitio estático (sin build command, output = raíz).
- Para actualizar: reemplazar `index.html` y hacer push.
- `vercel.json` desactiva el cache para que cada push se vea al instante.
